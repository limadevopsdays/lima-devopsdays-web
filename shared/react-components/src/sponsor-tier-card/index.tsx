import { createResponsiveStyled } from '@dyesthetics-lab/react-tv-variants-creators';

const SponsorTierCard = createResponsiveStyled({
  tag: 'div',
  preset: {
    base: [
      "rounded-3xl flex flex-col"
    ],
    variants: {

    }
  }
});

export default SponsorTierCard;